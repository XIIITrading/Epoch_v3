# Method Architecture Decisions Log

| Date | Decision | Rationale | Alternatives Considered |
|---|---|---|---|
| 2026-03-21 | Established _architecture/ auto-update workflow | Keep docs current without manual effort; ensure Claude Code always has accurate system context | Manual updates (too easy to skip), external tools like Miro (Claude Code can't read them) |
| 2026-03-21 | Renamed system from Epoch v3.0 to Method v1.0 | Fresh identity for the methodology-first rebuild; aligns codebase root (Method_v1) with system branding | Keep Epoch naming (confusing with V1/V2 legacy), version as Epoch v3 (implies continuity with different architecture) |
| 2026-03-21 | Dropped 7 orphan Supabase tables | Zero code references, 6 completely empty, 1 had 5 manual rows. Clearing dead scaffolding before baseline audit. Tables: ai_indicator_edges, ai_model_stats, ai_prompt_history, ai_recommendations, ai_zone_performance, trade_images, ramp_analysis_progression_patterns | Keep for future use (no code would ever reference them without new implementation), archive to backup (unnecessary overhead for empty tables) |
| 2026-03-21 | Retained 14 orphan-candidate tables pending V2 completion | Tables have data but no active code references. Dropping risks losing historical analysis that may be useful for V2 migration validation. Will reassess after all modules migrate to _2 tables. | Drop immediately (premature — some may contain unique historical data), migrate to archive schema (adds complexity without clear benefit) |
| 2026-03-21 | Approved Seed 002 — Architecture Baseline Audit | Architecture docs are ~40% accurate. 7/16 module names wrong, 55/75 tables undocumented, 8 modules missing CLAUDE.md. Every future build depends on accurate context. | Incremental fixes per module (too slow, inconsistencies compound), external wiki (Claude Code can't read it), skip and work from code only (defeats the purpose of the auto-update workflow) |
| 2026-03-21 | Established system_state/ versioned snapshot system | Architecture docs need to feed Claude Projects as context. Versioned snapshots (01.001, 01.002, etc.) provide immutable state captures that can be dropped into external Claude Project context. _architecture/ originals carry version numbers in headers. | Single-file export (too large), git tags only (Claude Projects can't read git), manual copy-paste (error-prone and tedious) |
| 2026-03-21 | State Update 01 — Corrected module naming across all architecture docs | 7 of 16 modules had wrong folder names in docs (07-14 except 10). Updated SYSTEM_MAP.md, root CLAUDE.md module map, all status fields. Module 04 corrected from "V1 Pending" to "V2 Active". Modules 08/10/11/15 corrected from "Planned" to "V2 Active". | Leave incorrect and fix incrementally (risks misleading Claude Code on every session) |
| 2026-03-22 | Completed Seed 003 — Screener Bucket Audit | 45 calculations mapped to 4 time-based buckets (Weekly/Nightly/Morning/Real-Time). 35 exist, 10 new. All 5 Supabase tables confirmed against live schema. Data contract for Seed 004. | Single audit vs two-phase (chose single — codebase analysis was already complete) |
| 2026-03-22 | Built Seed 004 Phase 1 — Bucket runners (A/B/C) with CLI entry point | Reduce morning prep from 30-45 min to <10 min by pre-computing all higher-TF data into Supabase overnight. Nightly wraps existing PipelineRunner headless. | Refactor PipelineRunner into separate calculators (rejected — too much risk). Batch API calls with asyncio (rejected — ThreadPoolExecutor simpler for network-bound work). |
| 2026-03-22 | Fixed Supabase exporter — per-ticker savepoints, dict-format support, session date extraction | Existing delete-then-insert lost data on partial failures. Index ticker results from model_dump() produced null ticker_id. Session date fell back to today(). | Rewrite exporter to V2 Supabase client (rejected — too large mid-build; savepoint fix is surgical). |
| 2026-03-22 | Parallelized options calculator — 4-worker ThreadPoolExecutor | 50-ticker universe: ~50 min sequential for options alone. Parallel: ~3-4 min. | In-pipeline parallelism (rejected — pre-compute is cleaner). Skip options for non-primary tickers (deferred). |
| 2026-03-22 | Deprecated LONG/SHORT composite scores (0-7) per SWH-6 | Scores already returning 0 since Feb 2026. Raw structure labels (BULL/BEAR/NEUTRAL) provide the same signal without abstraction. | Keep scores but recalculate (rejected — never validated by backtest). |
| 2026-03-22 | Pre-market window: 16:00 ET prior day to 07:30 ET current day | Broader than previous overnight window (20:00-12:00 UTC). Captures full after-hours + pre-market. | Keep existing overnight window (rejected — misses 16:00-20:00 ET after-hours). |
| 2026-03-22 | EMA 9/21 is Bucket D (real-time) only | EMA changes with every bar. Pre-computing stale EMA adds no value. | Pre-compute nightly EMA (rejected — hours stale by session open). |
| 2026-03-22 | Created Seeds 005 (Universe Ticker List) and 006 (Pre-Market Analysis Tool Beta) | Seed 005: 48 tickers provided, closed with `universe_tickers.txt`. Seed 006: PyQt6 morning workflow app consuming Seed 004 data. | Combine into Seed 004 (rejected — each has distinct scope). |
